# Foundry build artifacts
out/
cache/

# Keep broadcast receipts for on-chain audit trail; only skip dry-run runs.
broadcast/*/dry-run/

# Dependencies installed via `forge install` (re-fetched on demand)
lib/

# Local environment
.env
.env.*
!.env.example
